/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["github.com", "lh3.googleusercontent.com", "cdn.jsdelivr.net"],
  },
  /* config options here */
};

export default nextConfig;
